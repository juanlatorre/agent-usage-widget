# Source Claude accounts from separate profile directories

Each predefined Claude account slot connects to a distinct external Claude profile directory selected by the user. After explicit consent, the app imports that profile's credentials into a slot-specific Keychain entry and watches the selected source for changes, allowing the legacy profile and the team to remain independently addressable despite Claude Code's single default active identity.
